# Report Killer v0.2 - 测试指南

## 如何使用 Stdio 测试模式

Stdio 测试模式允许你在没有 API 密钥的情况下测试系统。你将手动扮演 LLM 的角色。

### 步骤 1：运行测试命令

```bash
report-killer test --test-mode
```

### 步骤 2：系统会显示提示词

系统会为每个插入点显示这样的提示：

```
======================================================================
STDIO TEST MODE - Simulating LLM
======================================================================
┌─ Prompt to LLM ────────────────────────────────┐
│ 你是一个专业的学术报告撰写助手。              │
│                                                │
│ 当前需要填写的内容：                           │
│ (1)                                            │
│                                                │
│ 在图1，3*3的方格棋盘上，摆放着1到8这八个数码...│
│                                                │
│ 请提供详细、专业的答案。注意：                 │
│ 1. 如果需要代码，请提供完整可运行的代码       │
│ 2. 如果需要实验结果，请说明如何获得           │
│ ...                                            │
└────────────────────────────────────────────────┘

Enter response (end with '===END==='):
```

### 步骤 3：输入你的回答

作为"LLM"，你应该输入一个合适的答案。例如：

```
八数码问题是一个经典的搜索问题，需要实现以下算法：

1. 广度优先搜索（BFS）
使用队列存储待扩展节点，按层次顺序搜索。

2. 深度优先搜索（DFS）
使用栈存储待扩展节点，深度优先探索。

3. A*启发式搜索
使用优先队列，结合g(n)和h(n)选择扩展节点。

以下是BFS的Python实现：

```python
from collections import deque

def bfs_solve(initial, goal):
    queue = deque([initial])
    visited = {initial}
    
    while queue:
        state = queue.popleft()
        if state == goal:
            return True
        
        for next_state in expand(state):
            if next_state not in visited:
                visited.add(next_state)
                queue.append(next_state)
    
    return False
```

===END===
```

### 步骤 4：系统处理响应

系统会：
1. 解析你的输入
2. 检测代码块（如果有）
3. 提取并执行代码（如果系统有相应的环境）
4. 将答案插入文档

你会看到：
```
Response received:
┌────────────────────────────────────────┐
│ 八数码问题是一个经典的搜索问题，需要... │
└────────────────────────────────────────┘

Executing python code...
✓ Execution successful
  ✓ Inserted 3 paragraphs
```

### 步骤 5：重复填写所有插入点

系统会依次为每个插入点显示提示，你需要为每个都提供答案。

### 步骤 6：查看结果

完成后，系统会显示：
```
Completion status:
  Total points: 3
  Filled: 3
  Remaining: 0
  Rate: 100.0%

✓ Document processed successfully!
Output saved to: documents/test_ai_doc_output.docx
```

## 示例会话

这是一个完整的测试会话示例：

```
$ report-killer test --test-mode

╭────────────────────────────────────────╮
│ 🧪 Report Killer - Test Mode           │
╰────────────────────────────────────────╯

Testing with: tests/test_ai_doc.docx
Output will be saved to: documents/test_ai_doc_output.docx
Running in STDIO test mode - you will act as the LLM

Code execution capabilities:
  ✓ C
  ✓ C++
  ✓ Python

Loading document: documents/test_ai_doc_output.docx
Document loaded successfully (1238 characters)

Found 3 insertion points:
  ○ [requirement] Para 22: (1)...
  ○ [section] Para 34: 1、实验报告需要包含以下几个部分...
  ○ [question] Para 42: 你所采用的估价函数f(n) = g(n) + h(n)中，g(n)和h(n)...

Processing insertion points...

Processing: (1)...

======================================================================
STDIO TEST MODE - Simulating LLM
======================================================================
[用户输入答案...]

  ✓ Inserted 2 paragraphs

Processing: 1、实验报告需要包含以下几个部分...
[用户输入答案...]

  ✓ Inserted 4 paragraphs

Processing: 你所采用的估价函数f(n) = g(n) + h(n)中，g(n)和h(n)...
[用户输入答案...]

  ✓ Inserted 3 paragraphs

Completion status:
  Total points: 3
  Filled: 3
  Remaining: 0
  Rate: 100.0%

✓ Test completed successfully!
Check the output at: documents/test_ai_doc_output.docx
```

## 提示

### 好的回答示例

对于问题："你所采用的估价函数f(n) = g(n) + h(n)中，g(n)和h(n)的主要作用是什么？"

**好的回答**：
```
在A*算法中，估价函数f(n) = g(n) + h(n)起到关键作用：

g(n)表示从起始节点到当前节点n的实际代价。它确保算法考虑已经走过的路径成本，保证了算法的完备性。如果g(n) = 0，算法退化为贪婪最佳优先搜索。

h(n)是从节点n到目标节点的估计代价，即启发函数。它指导搜索方向，使算法优先扩展更有希望的节点。一个好的h(n)能显著减少搜索空间。

两者结合：
- f(n)值小的节点会被优先扩展
- g(n)保证不忽略已付出的代价
- h(n)提供未来代价的估计
- 当h(n)满足可采纳性时，A*保证找到最优解
===END===
```

**不好的回答**：
```
g(n)是路径代价，h(n)是启发函数。
===END===
```
（太简短，没有实质内容）

### 包含代码的回答

对于要求编写代码的问题：

```
八数码问题的BFS实现如下：

```python
from collections import deque

def solve_8puzzle(initial, goal):
    """使用BFS求解八数码问题"""
    queue = deque([(initial, [])])
    visited = {initial}
    
    while queue:
        state, path = queue.popleft()
        
        if state == goal:
            return path
        
        for move, next_state in get_successors(state):
            if next_state not in visited:
                visited.add(next_state)
                queue.append((next_state, path + [move]))
    
    return None

def get_successors(state):
    """生成所有后继状态"""
    # 实现细节...
    pass
```

这个实现的时间复杂度是O(b^d)，其中b是分支因子，d是解的深度。
===END===
```

系统会自动检测并执行这段Python代码。

## 常见问题

### Q: 如何结束输入？

A: 在新的一行输入 `===END===` 然后按回车。

### Q: 可以在回答中包含代码吗？

A: 可以！使用 Markdown 代码块格式：
````
```python
print("Hello")
```
````

### Q: 系统会执行我的代码吗？

A: 会的，如果系统检测到相应的编译器/解释器。执行结果也会被插入到文档中。

### Q: 如果我不想某段代码被执行怎么办？

A: 不使用代码块格式，或者在不支持的语言中编写（系统会检测但不执行）。

### Q: 可以中途退出吗？

A: 按 Ctrl+C 可以中断程序，但已经填写的内容会丢失。

## 下一步

测试成功后，你可以：

1. 配置真实的 API 密钥
2. 使用生产模式处理文档
3. 查看生成的文档是否符合要求
4. 根据需要调整 custom_prompt

参见 README_v0.2.md 了解更多信息。
